<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /admin/
  # SPA: serve index.html for any path so client-side router can handle /admin/packages etc.
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^(.*)$ index.html [L]
</IfModule>
