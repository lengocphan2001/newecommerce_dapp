// Layout for product detail pages
// Note: generateStaticParams must be in page.tsx, not layout.tsx
export default function ProductDetailLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

