// Layout for order detail pages
// Note: generateStaticParams must be in page.tsx, not layout.tsx
export default function OrderDetailLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
